#include <stdio.h>
#include <stdlib.h>

int is_png(char* png) {
    FILE* check;

    /* open file */
    check = fopen(png, "r");

    /* check for error */
    if (check == NULL) {
        exit(1);
    }

    for () {
        
    }
}